// Function retruns ans array of the numbers between two given pararemeters inculuding parameters given.

function between(a, b) {
    let arr = []
    for (let i = a; i <= b; i++){
      arr.push(i)
    }
    return arr
  }