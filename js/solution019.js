// Function that takes 2 integers in the form of a stringanf outputs the sum in the form of a string

function sumStr(a,b) {
    let sumOfNums = Number(a) + Number(b)
    return sumOfNums.toString()
  }
  